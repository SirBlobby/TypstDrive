services:
  typstdrive:
    build: .
    container_name: typstdrive
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=sqlite:/app/data/typstdrive.db?mode=rwc
    volumes:
      - ./data:/app/data
    restart: unless-stopped
